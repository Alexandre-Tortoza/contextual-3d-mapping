# Run Contracts

Identidade de run compartilhada (`RunId`, `next_run_id`) e os nomes de subpasta padrão dentro de uma run (`ASSETS_DIRNAME`, `DEBUG_DIRNAME`) pertencem aqui.

Implementações concretas de composição, benchmark, experimento e publicação permanecem de posse de seus respectivos apps e módulos.

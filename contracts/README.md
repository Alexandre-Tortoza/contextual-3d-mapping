# Contracts

Shared, implementation-agnostic data contracts used to integrate independently developed modules. Contracts define data shape, invariants, provenance, confidence, coordinate frames, timestamps, and serialization boundaries.

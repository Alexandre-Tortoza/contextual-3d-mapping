[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "mapping-runtime"
version = "0.1.0"
description = "Offline composition for RGB LiDAR map slices."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/mapping_runtime"]
